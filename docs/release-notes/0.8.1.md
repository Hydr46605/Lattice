## Changed

- Added Maven Central publishing for `lattice-api`, `lattice-core`, and `lattice-paper`.
- Kept GitHub Packages as a permanent Maven mirror for the same artifacts.
- Updated developer documentation to use Maven Central as the default dependency source.
- Added Apache-2.0 project licensing metadata for Maven Central publication.

## Compatibility

- Maven coordinates remain unchanged: `dev.beryl:lattice-api`, `dev.beryl:lattice-core`, and `dev.beryl:lattice-paper`.
- Existing GitHub Packages consumers can keep using the authenticated GitHub Packages repository.
